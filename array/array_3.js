// No JavaScript, o array é um objeto que oferece operações para acessar e manipular suas propriedades

// É possível inicializar passando apenas um Number para a função construtora, que será o número de posições que terá esse Array

const languages = new Array(10);
console.log(languages);
